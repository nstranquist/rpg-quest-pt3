import { DefaultTheme } from 'styled-components'

// create custom theme by extending Default Theme
const rpgTheme: DefaultTheme = {
  colors: {
    primary: '#d95057',
    secondary: 'white',
  },
}

export { rpgTheme }
